QR CODE Spring Boot Sample.

This service generates a QR Textcode based on the string URI requested,

the service is at /code/{stringValue}


To Run:

java -jar demo.jar

To Compile:

mvn package -Dmaven.test.skip=true
